# Student Management Frontend

React frontend for entering and viewing student details.

## Run

```powershell
npm run dev:frontend
```

The frontend uses `VITE_API_URL` for the backend API. See `.env.example`.
